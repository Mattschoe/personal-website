// Stub — the real Projects index (alternating rows) lands in Phase 7.
export function Projects() {
  return (
    <section className="section">
      <div className="container">
        <span className="kicker">Projects</span>
        <h1>Coming soon</h1>
        <p className="lead">Projects will appear here once the content pipeline is wired up.</p>
      </div>
    </section>
  );
}
