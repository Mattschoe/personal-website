// Stub — the real Recipes index (filterable card grid) lands in Phase 6.
export function Recipes() {
  return (
    <section className="section">
      <div className="container">
        <span className="kicker">Recipes</span>
        <h1>Coming soon</h1>
        <p className="lead">Recipes will appear here once the content pipeline is wired up.</p>
      </div>
    </section>
  );
}
